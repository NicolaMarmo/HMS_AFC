#ifndef PERMUTE
#define PERMUTE

#include <cmath>
#include<vector>
#include "myRandom.h"


std::vector<int> i_rrSelect(const int& i_depth, const int& i_NP, const int& i_avoid);

std::vector<int> permutateIndexFull(int i_NP);


#endif

